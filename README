* First run 'mvn clean install' to build the project.
* Run jetty:run maven goal to view the demo application at, http://localhost:9000/filteringtabledemo/
* To debug the app, run one of jetty_debug scripts and connect with eclipse to port 4000.